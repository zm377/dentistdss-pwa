import { Container, Typography } from '@mui/material';

function Book() {
    return (
        <Container>
            <Typography variant="h4" component="h1" gutterBottom>
                Book
            </Typography>
        </Container>
    );
}

export default Book;
